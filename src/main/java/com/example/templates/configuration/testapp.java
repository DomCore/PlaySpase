package com.example.templates.configuration;

import java.util.ArrayList;
import java.util.Arrays;

import java.lang.reflect.Array;

import com.example.templates.model.Category;

public class testapp {
/*  public static void main(String args[]) {
    Category category = new Category();
    category.setTemplates(Arrays.asList("Первый","Второй","","Gold","Silver",""));
    category.setTemplate("Сервер,Ранг,Золото" + category.getTemplates());
    StringBuilder a = new StringBuilder();
    String subTemp;
    String[] temp = category.getTemplate().split(",");
    for (int j = 0; j < temp.length; j++) {
      a.append(temp[j] + ": ");
      for (int i = 0; i < category.getTemplates().size(); i++) {
        subTemp = category.getTemplates().get(i);
        if (subTemp.length() > 0) {

        }
      }
    }


    System.out.println("Sum of x+y = ");
  }*/
}
